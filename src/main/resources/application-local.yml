spring:
  config:
    activate:
      on-profile: local

  datasource:
    url: jdbc:mariadb://127.0.0.1:3306/hometact
    driver-class-name: org.mariadb.jdbc.Driver
    username: username
    password: password